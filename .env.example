# Buttondown API key (write-only subscriber key, see Settings → API → Keys)
# Do NOT put a real key here. Add it as a GitHub Actions secret named BUTTONDOWN_API_KEY.
VITE_BUTTONDOWN_API_KEY=your_buttondown_api_key_here
